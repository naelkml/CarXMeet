{% extends 'base.html.twig' %}

{% block title %}
    CarXMeet - Modifier événement
{% endblock %}

{% block body %}
    <main style="min-height: 100vh; display: grid; place-items: center; padding: 24px;">
        <section style="width: 100%; max-width: 720px; border: 1px solid #e5e7eb; border-radius: 12px; padding: 24px;">
            <header style="display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-bottom: 16px;">
                <h1 style="margin: 0; font-size: 22px;">Modifier événement</h1>
                <a href="{{ path('app_events_show', { id: event.id }) }}" style="font-size: 14px; text-decoration: none; color: #111827;">
                    Retour
                </a>
            </header>

            {% for message in app.flashes('success') %}
                <div style="margin: 0 0 12px; padding: 10px 12px; border: 1px solid #bbf7d0; background: #f0fdf4; color: #166534; border-radius: 10px;">
                    {{ message }}
                </div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div style="margin: 0 0 12px; padding: 10px 12px; border: 1px solid #fecaca; background: #fef2f2; color: #991b1b; border-radius: 10px;">
                    {{ message }}
                </div>
            {% endfor %}

            {% set img64 = event.imageBase64 %}
            {% if img64 %}
                {% set coverSrc = 'data:image/jpeg;base64,' ~ img64 %}
                <a class="cxm-mediaItem cxm-cover" data-cxm-lb="1" href="{{ coverSrc }}" data-cxm-caption="Cover {{ event.title }}" style="margin-bottom: 12px;">
                    <img src="{{ coverSrc }}" alt="Cover {{ event.title }}" loading="lazy" decoding="async">
                </a>
            {% endif %}

            {% if event.galleryPhotos|length %}
                <div style="margin: 0 0 14px; padding: 12px; border: 1px solid #e5e7eb; border-radius: 12px;">
                    <h2 style="margin: 0 0 10px; font-size: 16px;">Galerie ({{ event.galleryPhotos|length }}/8)</h2>
                    <div class="cxm-galleryGrid">
                        {% for p in event.galleryPhotos %}
                            {% set p64 = p.imageBase64 %}
                            <div class="cxm-tile">
                                <div class="cxm-tileMedia">
                                    {% if p64 %}
                                        {% set pSrc = 'data:image/jpeg;base64,' ~ p64 %}
                                        <a class="cxm-mediaItem" data-cxm-lb="1" href="{{ pSrc }}" data-cxm-caption="{{ event.title }}">
                                            <img src="{{ pSrc }}" alt="Photo {{ event.title }}" loading="lazy" decoding="async">
                                        </a>
                                    {% endif %}
                                </div>
                                <div class="cxm-tileActions">
                                    <form method="post" action="{{ path('app_events_photo_delete', { id: event.id, photo: p.id }) }}" onsubmit="return confirm('Supprimer cette photo ?');">
                                        <input type="hidden" name="_token" value="{{ csrf_token('delete_event_photo_' ~ p.id) }}">
                                        <button type="submit" style="width: 100%; padding: 8px 10px; border: 0; border-radius: 10px; background: #991b1b; color: white; cursor: pointer;">
                                            Supprimer
                                        </button>
                                    </form>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            {% endif %}

            {% include 'events/_form.html.twig' with { form: form } %}
        </section>
    </main>
{% endblock %}
